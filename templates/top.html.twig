{% import "macro.html.twig" as macro %}
<table id="top20Desc" class="topResult">
    <tr><td>Établissement</td><td>Total</td><td>Élèves</td><td>Enseignants</td><td>Autres</td></tr>
    {% for line in table %}
    <tr>
        <td>{{ line.nom }}</td>
        <td>{{ line.total }}</td>
        <td>{{ macro.ratio(line.totalEleves, line.eleves) }}</td>
        <td>{{ macro.ratio(line.totalEnseignants, line.enseignants) }}</td>
        <td>{{ macro.ratio(line.totalAutres, line.autres) }}</td>
    </tr>
    {% endfor %}
</table>